<h1>messageList</h1>
<ul>
    <% @messages.each do |message| %>
        <li>
            <%= link_to message.id, message %>
            <%= message.content %>
        </li>
    <% end %>
    
</ul>

<%= link_to '新規メッセージの作成', new_message_path %>
