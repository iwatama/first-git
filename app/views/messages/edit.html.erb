<h1>message update
<br>
<%= message.id %>のupdate page
</h1>

    <%= form_for(@message) do |f| %>
    <%= f.label :content, 'メッセージ' %>
    <%= f.text_field :content %>

    <%= f.submit 'toukou' %>
 <% end %>


<%= link_to 'back to list', messages_path %>

